<%= form_for(@reports_download) do |f| %>
  <% if @reports_download.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reports_download.errors.count, "error") %> prohibited this reports_download from being saved:</h2>

      <ul>
      <% @reports_download.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
